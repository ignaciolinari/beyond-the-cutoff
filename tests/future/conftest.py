# This folder contains tests for scripts that have been moved to scripts/future/
# These tests are not currently active and should be skipped by pytest.
# To run these tests, the corresponding scripts need to be moved back to active status.

collect_ignore_glob = ["test_*.py"]
