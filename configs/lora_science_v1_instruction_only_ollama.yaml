provider: ollama
# Model tag for instruction-only fine-tuned model (trained without RAG contexts)
# This model is used for FT-only evaluation
model: lora_science_0p5_instruction_only
model_type: instruction_only  # Explicitly mark as instruction-only trained model
host: http://localhost
port: 11434
timeout: 480.0
device: auto
torch_dtype: auto
max_new_tokens: 512
temperature: 0.0
top_p: 0.9
repetition_penalty: 1.05
stop_sequences:
  - "<|im_start|>"
  - "<|im_end|>"
